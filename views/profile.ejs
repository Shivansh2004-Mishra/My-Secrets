<%- include('partials/header') %>

<div class="container mt-5">
   <h3 class="text" >Welcome to your profile page!</h3>
  <div class="card mx-auto" style="max-width: 400px;">
    <div class="card-body text-center">
      <i class="fa-solid fa-user-circle fa-5x mb-3"></i>
      <h2 class="card-title mb-2"><%= user.name %></h2>
      <h5 class="card-subtitle mb-3 text-muted"><%= user.email %></h5>
       
      <a href="/submit" class="btn btn-primary mt-3">Submit a Secret</a>
      <a href="/logout" class="btn btn-secondary mt-3">Logout</a>
    </div>
  </div>
</div>

<%- include('partials/footer') %>